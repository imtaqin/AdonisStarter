# Dependencies and AdonisJS build
node_modules
build
tmp/*
!tmp/.gitkeep

# Secrets
.env
.env.local
.env.production.local
.env.development.local

# Frontend assets compiled code
public/assets

# Build tools specific
npm-debug.log
yarn-error.log

# Editors specific
.fleet
.idea
.vscode
# ...but the MCP config is shared tooling, not a personal editor setting.
# Generated by scripts/sync-agent-configs.mjs
!.vscode/mcp.json

# Platform specific
.DS_Store

# Playwright MCP scratch output
.playwright-mcp/

# Font Awesome Pro source bundle (~1.1GB, mostly SVG/sprite sources we do not
# ship). The 6MB of webfonts + CSS we actually use IS committed, under
# public/theme/fontawesome. Re-create this folder from your Font Awesome
# download and run `npm run theme:fontawesome` if you need to re-vendor.
fontawesome-pro-7.2.0-web/
