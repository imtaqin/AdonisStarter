@let(descriptionFromSlot = await $slots.main())
@let(description = descriptionFromSlot.trim() ? descriptionFromSlot : $props.get('text'))
@let(classes = ['mb-0'])

<div {{ $props.except(['text']).merge({ class: classes }).toAttrs() }}>{{{ description }}}</div>
